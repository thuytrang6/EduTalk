import 'package:flutter/material.dart';

class LichSuScreen extends StatelessWidget {
  const LichSuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Trang Lịch Sử", style: TextStyle(fontSize: 22)),
    );
  }
}
